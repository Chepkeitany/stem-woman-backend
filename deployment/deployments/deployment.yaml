apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: stem-woman
spec:
  replicas: 1
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: stem-woman
        tier: backend
        type: api
        name: api
    spec:
      restartPolicy: Always
      containers:
      - name: stem-woman
        image: gcr.io/kubernetes-intro-226510/stem-woman:1.0.10
        readinessProbe:
          httpGet:
            path: /health
            port: 1500
            scheme: HTTP
        livenessProbe:
          httpGet:
            path: /health
            port: 1500
            scheme: HTTP
          initialDelaySeconds: 30
          timeoutSeconds: 5
        envFrom:
          - secretRef:
              name: stem-woman-secrets
          - secretRef:
              name: cloudsql-db-credentials
          - configMapRef:
              name: stem-woman-configmaps
        ports:
          - containerPort: 1500
      - name: cloudsql-proxy
        image: gcr.io/cloudsql-docker/gce-proxy:1.12
        command:
          - /cloud_sql_proxy
          - -instances=kubernetes-intro-226510:europe-west1:stem-woman=tcp:5432
          - -credential_file=/secrets/cloudsql/credentials.json
        volumeMounts:
          - name: cloudsql-instance-credentials
            mountPath: /secrets/cloudsql
            readOnly: true
      volumes:
        - name: cloudsql-instance-credentials
          secret:
            secretName: cloudsql-instance-credentials
